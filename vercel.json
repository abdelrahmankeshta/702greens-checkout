{
    "buildCommand": "cd 702greens-checkout-nextjs && npm install && npm run build",
    "outputDirectory": "702greens-checkout-nextjs/.next",
    "framework": "nextjs",
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/api/server"
        }
    ]
}